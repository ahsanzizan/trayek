import { describe, it } from "vitest";

describe("INV-1: Human approval for invoices and faktur pajak", () => {
  it.todo(
    "an invoice or faktur pajak cannot leave DRAFT without actor, timestamp, and IP",
  );
});
