import { describe, it } from "vitest";

describe("INV-2: Human action for collections messages", () => {
  it.todo(
    "a collections message cannot reach an external recipient without a per-message human send action",
  );
});
