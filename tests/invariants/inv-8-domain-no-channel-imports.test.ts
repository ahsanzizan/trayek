import { describe, it } from "vitest";

describe("INV-8: Core domain does not import channel adapters", () => {
  it.todo(
    "the core domain remains independent from src/server/channels/* adapters",
  );
});
